# SubBoost v1.4.1

## 新增

* 新增“方法三：手动添加规则集”。
* 支持 `.mrs`、`.yaml`、`.yml` 格式。
* 支持 `domain`、`ipcidr` 和 YAML `classical` 类型。
* 支持选择目标代理组，并在原有规则集列表中管理、排序和删除。

## 优化

* 兼容现有官方 MRS 转 YAML 功能。
* 第三方 MRS 和原生 YAML 规则集保持原格式。
* 规则集数据可随订阅、模板、同步及数据库备份完整保存。
* 兼容旧版规则集数据，无需数据库迁移。
* 方法一和方法二的原有逻辑及样式保持不变。
