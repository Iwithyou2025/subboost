# SubBoost v1.0.0

## 中文

### 新增

- 新增 YAML 规则集输出。
- 新增 MRS / YAML 规则集切换。
- YAML 规则集选择支持持久化。

### 修复

- 修复仪表盘链接与 YAML 规则集选择不一致的问题。

## English

### Features

- Added YAML rule-provider output.
- Added MRS / YAML rule-provider switching.
- Persisted YAML rule-provider preference.

### Fixes
- Fix the issue of inconsistency between dashboard links and YAML rule set selection.
